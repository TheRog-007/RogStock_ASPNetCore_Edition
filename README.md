Conversion of RogStock 2024 into ASp MVC EF!

Uses SAME SQl Server database as its WinForms parent
Uses custom ViewModels
Uses custom classes

As project progrsses will morph into something that looks less and less from the outside than the standard
ASP view!

